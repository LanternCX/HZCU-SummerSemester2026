# Task Completion

- For documentation-only changes: verify file list with `find docs/reference -type f | sort` and check stale links/old paths with `rg`.
- For current repo state: no compiler, test runner, linter, or formatter is configured.
- Before future Java implementation completion, add and run the project build/test command, then update this memory.
- If Serena memories changed, user can run `serena memories check` from project root.