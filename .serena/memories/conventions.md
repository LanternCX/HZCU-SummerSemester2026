# Conventions

- Documentation filenames and directories should be short English names; Chinese content is allowed.
- Keep root `README.md` as the human entry point and `docs/reference/README.md` as the reference index.
- Do not maintain duplicate original course documents alongside converted reference docs.
- Prefer the minimal Swing/JDBC implementation path matching course expectations; avoid extra frameworks unless requested.