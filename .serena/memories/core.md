# Core

- Repository currently contains documentation/reference material only; no application source tree yet.
- Top-level entry: `README.md` links the canonical reference docs under `docs/reference`.
- Single documentation source invariant: keep docs in `docs/reference`; do not recreate parallel original-doc folders under `docs`.
- Read tech stack details in `mem:tech_stack` before implementation work.
- Read completion checks in `mem:task_completion` before claiming coding work is done.